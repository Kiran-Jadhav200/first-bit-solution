#include<stdio.h>
//check if person is  eligible for voting or not
void main(){

    int age=24;
   // scanf("%d",age);
    int ans = age >=18;
    printf("the person is eligible for voting if ans is 1 and not is 0: %d",ans);
}